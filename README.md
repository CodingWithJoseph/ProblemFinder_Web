# ProblemFinder Web

Modern Next.js 14 frontend scaffold for the ProblemFinder platform.

## Working with this branch

```bash
git checkout feature/frontend_structure_v2
npm install
npm run dev
```

The development server runs at [http://localhost:3000](http://localhost:3000).
